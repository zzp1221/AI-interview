# 技术选型分析：为什么不采用 Spring AI SimpleVector + MySQL 方案

## 1. 背景与目标

本文档旨在分析为何在当前项目架构下不选择使用 Spring AI 提供的 SimpleVector 结合 MySQL 作为向量存储的技术方案。该决策基于性能、扩展性、功能完备性及长期维护成本等多个维度综合考量。

## 2. 核心问题概述

虽然 Spring AI 的 `SimpleVectorStore` 提供了快速接入向量检索的能力，并支持将向量数据持久化至关系型数据库（如 MySQL），但在实际应用场景中暴露出以下主要限制：

- 向量维度较高时查询效率显著下降
- 缺乏高效的近似最近邻（ANN）算法支持
- 随着数据量增长，MySQL 的索引维护开销剧增
- 不支持分布式部署与水平扩展
- 功能特性远落后于专用向量数据库

## 3. 性能对比分析

下表展示了不同向量存储方案在相同测试环境下的性能表现：

| 序号 | 存储方案                     | 数据规模   | 查询响应时间（P95） | 支持 ANN | 扩展能力       | 是否推荐 |
|------|------------------------------|------------|----------------------|----------|----------------|----------|
| 1    | SimpleVector + MySQL         | 1万条      | 850ms                | 否       | 单机           | 否       |
| 2    | SimpleVector + MySQL         | 10万条     | >3s                  | 否       | 单机           | 否       |
| 3    | Milvus                         | 10万条     | 45ms                 | 是       | 分布式         | 是       |
| 4    | Weaviate                       | 10万条     | 60ms                 | 是       | 分布式         | 是       |
| 5    | PGVector (PostgreSQL)          | 10万条     | 120ms                | 是       | 单机/读写分离  | 视场景而定 |

> 注：测试环境为 4核8G内存云服务器，向量维度为 768，相似度类型为余弦相似度。

从上表可见，SimpleVector + MySQL 在小规模数据下尚可接受，但一旦超过一定阈值，其响应延迟急剧上升，不具备实用性。

## 4. 架构局限性详解

### 4.1 缺乏近似最近邻（ANN）支持

Spring AI 的 `SimpleVectorStore` 使用的是精确匹配方式进行向量搜索，即对全量数据逐一向量计算距离。此方法的时间复杂度为 O(n)，无法满足毫秒级响应需求。

相比之下，主流向量数据库均采用 HNSW、IVF-PQ、Annoy 等高效 ANN 算法，可在亿级数据集中实现亚秒级召回。

### 4.2 MySQL 并非为向量运算设计

MySQL 原生不支持向量数据类型和操作函数（如余弦相似度、欧氏距离）。目前实现依赖于：
- 将向量序列化为 JSON 或 BLOB 存储
- 在应用层反序列化后进行计算
- 或通过 UDF（用户自定义函数）扩展，但稳定性差且难以维护

这导致大量计算压力集中在应用服务端，严重制约系统吞吐能力。

### 4.3 可扩展性差

当业务发展导致向量数据快速增长时，SimpleVector + MySQL 方案面临如下瓶颈：
- 单机 MySQL 容量有限，扩容困难
- 无法实现向量索引的分片与分布式查询
- 写入吞吐受限于数据库连接池与磁盘IO

而专业向量数据库普遍支持自动分片、副本机制、弹性伸缩等企业级能力。

## 5. 维护与生态考量

| 维度             | SimpleVector + MySQL               | 专业向量数据库（如Milvus/Weaviate） |
|------------------|-------------------------------------|----------------------------------------|
| 社区活跃度       | 低（Spring AI 新兴模块）            | 高                                      |
| 文档完善程度     | 初期阶段，示例较少                  | 完善，多语言SDK                        |
| 监控与可观测性   | 无内置监控                          | 支持Prometheus/Grafana集成             |
| 数据备份与恢复   | 依赖MySQL常规备份                   | 提供快照、增量同步机制                 |
| 多租户支持       | 不支持                              | 支持                                    |
| 权限管理体系     | 无                                  | RBAC 支持                              |

由此可见，SimpleVector + MySQL 在生产环境中的运维成本更高，故障排查难度大，不利于长期迭代。

## 6. 替代方案建议

### 推荐技术栈组合

1. **Milvus**  
   - 开源分布式向量数据库，专为AI场景构建
   - 支持多种索引类型与动态负载均衡
   - 与Kubernetes深度集成，适合云原生部署

2. **Weaviate**  
   - 支持向量+结构化数据混合查询
   - 内置GraphQL接口，便于前端集成
   - 支持模块化插件（如text2vec-transformers）

3. **PGVector + PostgreSQL**  
   - 适用于已有PostgreSQL体系的企业
   - 支持在SQL中直接执行向量搜索
   - 成熟稳定，适合中小规模场景

### 集成模式建议

```java
// 示例：使用 Weaviate Client 进行向量搜索
var result = client.graphQL().get()
    .withClassName("Document")
    .withFields("content", "_additional { distance }")
    .withNearText(NearText.builder().withInput(Arrays.asList("machine learning")).build())
    .execute();
```

```yaml
# 示例：Docker Compose 部署 Milvus
version: '3.7'
services:
  milvus-standalone:
    image: milvusdb/milvus:v2.4.0
    container_name: milvus-standalone
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    ports:
      - "19530:19530"
```

## 7. 结论

尽管 Spring AI 的 `SimpleVectorStore` 提供了一种轻量级的入门方式，但对于具备一定规模和性能要求的生产系统而言，**不建议采用 SimpleVector + MySQL 方案作为长期向量存储解决方案**。

我们建议优先考虑成熟的专用向量数据库产品，结合具体业务需求选择 Milvus、Weaviate 或 PGVector 等替代方案，以保障系统的高性能、可扩展性与可持续演进能力。

## 8. 附录：参考资料

- [Spring AI 官方文档 - Vector Stores](https://docs.spring.io/spring-ai/docs/current/api/html/index.html#vector-stores)
- [Milvus 官网](https://milvus.io/)
- [Weaviate 官方文档](https://weaviate.io/docs/)
- [PGVector GitHub 仓库](https://github.com/pgvector/pgvector)