# 智能面试指导系统架构文档

## 一、系统总体设计概览

本系统为一个基于人工智能的智能面试指导平台，旨在为用户提供从简历优化、模拟面试到知识库问答的一站式求职辅助服务。系统采用现代化的前后端分离架构，后端基于 **Spring Boot** 框架构建，前端使用 **React** 与 **TypeScript** 开发，确保了良好的可维护性与扩展性。

系统核心功能模块化设计，主要包含以下三大业务板块：

- **简历分析模块**：支持用户上传简历文件（如PDF、Word），通过AI技术自动解析内容，并进行智能评分与改进建议生成。
- **模拟面试模块**：根据用户简历和目标岗位，动态生成技术与行为类问题，提供沉浸式面试体验，并对回答进行实时评估。
- **知识库问答模块**：集成 **RAG（检索增强生成）** 技术，允许用户上传自定义文档构建个人知识库，并通过聊天界面进行智能问答。

关键技术特性贯穿整个系统设计：

- **AI深度集成**：所有核心功能均由大语言模型（LLM）驱动，通过预设的提示词模板（Prompt Templates）精确控制AI输出，确保结果的专业性与一致性。
- **异步非阻塞处理**：利用 **Redis Stream** 实现任务队列，将耗时操作（如简历分析、文档向量化、答案评估）异步化，显著提升系统响应速度与用户体验。
- **全栈类型安全**：前端采用 TypeScript 定义数据结构，后端使用 Java 强类型语言，配合统一的 `Result<T>` 响应封装，保障了前后端接口的健壮性与可靠性。

该架构设计兼顾了性能、可扩展性与开发效率，为构建智能化、高可用的面试指导服务提供了坚实的技术基础。

## 二、系统分层架构与职责划分

系统采用清晰的纵向分层架构设计，各层级职责分明，有效实现了关注点分离（Separation of Concerns），提升了代码的可维护性与可测试性。整体架构遵循标准的后端工程实践，从上至下分为七个核心层级。

以下表格系统化地整理了各层级的目录结构及其核心职责：

| 层级 | 目录 | 职责 |
| --- | --- | --- |
| **Controller** | `modules/*/controller` | 作为 REST API 的入口，负责接收 HTTP 请求，进行参数校验，并调用对应的 Service 层方法处理业务逻辑。 |
| **Service** | `modules/*/service/` | 承载核心业务逻辑，是系统功能实现的关键层。负责协调多个 Repository 操作，并管理事务边界。 |
| **Repository** | `modules/*/repository/` | 数据访问层，负责与数据库进行交互。基于 JPA 规范实现实体的增删改查及复杂查询操作。 |
| **Model** | `modules/*/model/` | 定义数据模型，包含持久化 Entity 实体类以及用于接口传输的 DTO（Data Transfer Object）对象。 |
| **Listener** | `modules/*/listener/` | 异步消息消费者，监听 Redis Stream 中的消息，执行简历分析、答案评估等耗时任务，实现非阻塞处理。 |
| **Infrastructure** | `infrastructure/` | 提供通用技术基础设施支持，包括文件解析（Apache Tika）、S3 文件存储、Redis 操作封装、PDF 导出服务等。 |
| **Common** | `common/` | 存放全系统共享的公共组件，如全局异常处理器、常量定义、统一响应结果封装 `Result<T>` 及 CORS 配置等。 |

该分层模式确保了业务逻辑的独立性与系统的可扩展性。例如，当需要新增“笔试测评”模块时，只需在 `modules/` 下创建对应目录并遵循此分层规范即可快速集成，而无需修改现有核心逻辑。

## 三、核心业务模块功能说明

系统围绕求职者的核心需求，构建了三大高内聚、低耦合的业务模块：简历模块、面试模块与知识库模块。每个模块均遵循统一的分层架构规范，实现了功能的清晰划分与高效协作。

### 简历分析模块

该模块负责处理用户上传的简历文件，并通过AI技术进行智能解析与评分。

- **ResumeController**：作为API入口，接收简历上传请求。
- **ResumeUploadService**：执行文件上传逻辑，调用基础设施层的服务进行类型校验与S3存储。
- **AnalyzeStreamProducer**：在文件上传后，向 `analyze_stream` Redis Stream 发送异步分析任务。
- **AnalyzeStreamConsumer**：监听消息队列，触发实际的分析流程。
- **ResumeGradingService**：利用AI模型与`resume-analysis-*.st`提示词模板，对简历内容进行技能提取、经历评估与综合打分。
- **ResumePersistenceService**：将分析结果通过JPA Repository持久化至数据库。

整个流程采用异步解耦设计，确保前端上传后能立即获得响应，而耗时的AI分析在后台完成。

### 面试模拟模块

本模块提供沉浸式的模拟面试体验，支持动态提问与实时反馈。

- **InterviewController**：暴露模拟面试相关的REST接口。
- **InterviewSessionService**：管理用户的面试会话状态，维护上下文一致性。
- **InterviewQuestionService**：基于用户简历和目标岗位，结合`interview-question-*.st`提示词模板，生成个性化的问题序列。
- **AnswerEvaluationService**：接收用户回答，启动评估流程。
- **EvaluateStreamConsumer**：从 `evaluate_stream` 消费任务，使用`interview-evaluation-*.st`提示词对回答的语言表达、技术深度等维度进行AI评估。

该模块支持SSE（Server-Sent Events），可实现面试问题与评估结果的流式推送，提升交互流畅度。

### 知识库问答模块

此模块集成RAG（检索增强生成）技术，使系统能够基于用户自定义的知识文档进行智能问答。

- **KnowledgeBaseController**：提供知识库文档的上传与管理API。
- **KnowledgeBaseUploadService**：处理文档上传，调用Apache Tika进行内容解析。
- **VectorizeStreamConsumer**：监听 `vectorize_stream`，消费文档向量化任务。
- **KnowledgeBaseVectorService**：将解析后的文本块转换为向量表示。
- **VectorRepository**：将向量数据存入支持pgvector扩展的PostgreSQL数据库，实现高效相似度检索。
- **RagChatController**：提供支持SSE的聊天接口，实现实时问答流。
- **KnowledgeBaseQueryService**：在收到查询时，先检索相关文档片段，再结合`knowledgebase-query-*.st`提示词与LLM生成最终回答。
- **RagChatSessionService**：管理问答会话的上下文状态，确保多轮对话连贯性。

该模块充分利用异步处理与向量数据库，保证了知识库构建的效率与问答的准确性。

## 四、AI能力与异步处理机制

本系统通过两大核心技术支柱实现智能化与高性能：**AI驱动的决策引擎**与**基于消息队列的异步处理架构**。二者协同工作，确保复杂AI任务高效执行的同时，维持前端交互的实时性与流畅度。

### AI能力集成：提示词工程驱动智能服务

系统的全部AI功能均通过外部化的提示词模板（Prompt Templates）进行精确控制，所有模板集中存放于 `src/main/resources/prompts/` 目录下，实现了逻辑与配置的解耦，便于维护与迭代。

以下表格归纳了核心功能模块所依赖的提示词文件及其作用：

| 功能模块 | 提示词文件 | 核心作用 |
| --- | --- | --- |
| 简历分析 | `resume-analysis-*.st` | 指导AI模型提取简历中的技能、项目经历，并进行综合评分与改进建议生成 |
| 面试问题生成 | `interview-question-*.st` | 基于用户简历内容与目标岗位，动态生成技术深度与行为表现类面试题 |
| 面试评估 | `interview-evaluation-*.st` | 对用户的口语回答进行多维度评价，包括语言表达、逻辑结构、技术准确性等 |
| 知识库问答 | `knowledgebase-query-*.st` | 控制RAG流程中检索后的内容生成，确保回答格式规范且信息准确 |

该设计使AI行为具备高度可预测性与可审计性，是系统实现专业级输出的关键保障。

### 异步处理机制：Redis Stream解耦耗时任务

为避免阻塞主线程影响用户体验，系统将所有计算密集型操作异步化，采用 **Redis Stream** 作为消息中间件，构建生产者-消费者模式的任务队列。

关键异步任务流如下表所示：

| 任务类型 | 触发条件 | 执行方式 | 目标组件 |
| --- | --- | --- | --- |
| 简历分析 | 用户完成简历上传 | 异步消费 `analyze_stream` 消息 | `AnalyzeStreamConsumer` 调用 `ResumeGradingService` |
| 答案评估 | 用户提交面试回答 | 异步消费 `evaluate_stream` 消息 | `EvaluateStreamConsumer` 启动AI评估流程 |
| 文档向量化 | 用户上传知识库文档 | 异步消费 `vectorize_stream` 消息 | `VectorizeStreamConsumer` 调用嵌入模型生成向量 |

所有异步操作由 `RedisService` 统一封装，并利用 `InterviewSessionCache` 缓存会话状态，确保任务执行过程中的上下文一致性。该机制显著提升了系统的吞吐能力与响应速度。


