# AI-interview 与 interview-guide-master 功能差异分析报告

## 1. 对比范围与说明

- 当前项目：`d:\项目\AI-interview\src` 与 `d:\项目\AI-interview\frontend\src`
- 原型项目：`d:\项目\AI-interview\interview-guide-master\app\src` 与 `d:\项目\AI-interview\interview-guide-master\frontend\src`
- 已按你的要求忽略：
  - Maven / Gradle 构建系统差异
  - 你项目中新增的“按用户区分”能力及相关鉴权页面/接口
- 对比方法：
  - 文件级全量差异扫描（前端、后端、resources）
  - 关键入口与控制器内容级对比（路由、API、配置）

---

## 2. 结论总览

原型新增能力中，你的项目当前主要缺失以下 3 条主线：

1. 面试方向（Skill）体系与 JD 解析链路（前后端都缺）
2. 面试日程管理模块（前后端都缺）
3. 语音面试全链路（WebSocket + ASR/TTS + 评估）（前后端都缺）

此外，文本面试与配置层还有若干中等差异（如会话列表接口、Prompt 拆分、部分配置类）。

---

## 3. 详细差异清单（需补齐）

## 3.1 前端缺失功能

### A. 模拟面试入口与配置能力升级缺失

- 原型有独立“面试中心/模拟面试 Hub”，支持：
  - 文字/语音模式切换
  - 技能方向选择
  - 自定义 JD 解析后生成分类
  - 难度、题量、时长、简历关联等配置
- 你当前项目仍是“进入文字面试页后配置”的旧流。

关键证据：
- 原型路由包含 `interview-hub`、`voice-interview`、`interview-schedule`：`interview-guide-master/frontend/src/App.tsx`
- 当前路由缺失上述入口：`frontend/src/App.tsx`

### B. 语音面试页面与评估页缺失

- 原型有：
  - `VoiceInterviewPage`（实时语音会话）
  - `VoiceInterviewEvaluationPage`（异步评估轮询与报告）
  - 实时字幕、录音、音频播放组件
- 你当前项目无对应页面与组件链路。

缺失文件族：
- `api/voiceInterview.ts`
- `pages/VoiceInterviewPage.tsx`
- `pages/VoiceInterviewEvaluationPage.tsx`
- `components/AudioRecorder.tsx`、`AudioPlayer.tsx`、`RealtimeSubtitle.tsx`

### C. 面试日程前端能力缺失

- 原型有完整日程管理页和日历组件，支持新增/编辑/删除/状态更新/拖拽调整。
- 你当前项目无该页面与 API。

缺失文件族：
- `api/interviewSchedule.ts`
- `pages/InterviewSchedulePage.tsx`
- `components/interviewschedule/*`
- `hooks/useInterviewSchedule.ts`
- `types/interviewSchedule.ts`

### D. 面试记录聚合能力缺失（文字+语音统一）

- 原型面试记录页可聚合展示文字面试和语音面试，并支持“继续面试/重新面试/类型筛选”。
- 你当前仅覆盖文字面试记录视图。

---

## 3.2 后端缺失功能

### A. Skill 模块缺失（面试方向管理 + JD 解析）

- 原型提供：
  - `GET /api/interview/skills`
  - `GET /api/interview/skills/{id}`
  - `POST /api/interview/skills/parse-jd`
- 当前项目无 `modules/interview/skill` 包及对应服务。

关键证据：
- 原型控制器：`interview-guide-master/app/src/main/java/interview/guide/modules/interview/skill/InterviewSkillController.java`
- 当前项目无对应控制器/服务。

### B. 面试日程模块缺失

- 原型提供完整 REST：
  - `POST /api/interview-schedule/parse`
  - `POST /api/interview-schedule`
  - `GET /api/interview-schedule`
  - `GET /api/interview-schedule/{id}`
  - `PUT /api/interview-schedule/{id}`
  - `DELETE /api/interview-schedule/{id}`
  - `PATCH|PUT /api/interview-schedule/{id}/status`
- 当前项目无 `modules/interviewschedule` 相关实体、仓储、服务、控制器。

关键证据：
- 原型控制器：`interview-guide-master/app/src/main/java/interview/guide/modules/interviewschedule/InterviewScheduleController.java`

### C. 语音面试模块缺失

- 原型完整覆盖：
  - 会话创建/恢复/暂停/继续/结束/删除
  - 会话消息拉取
  - 异步评估触发与状态查询
  - WebSocket 实时语音对话
- 当前项目无 `modules/voiceinterview` 整体模块。

关键证据：
- 原型控制器：`interview-guide-master/app/src/main/java/interview/guide/modules/voiceinterview/controller/VoiceInterviewController.java`
- 原型还包含 `config`、`handler`、`listener`、`repository`、`service` 全套实现。

### D. 文本面试 API 存在中等差异

- 原型有 `GET /api/interview/sessions`（文字面试会话列表）。
- 你当前 `InterviewController` 中缺失该接口，导致原型“统一面试记录聚合”能力不可直接复用。

---

## 3.3 Resources / Prompt / 配置层缺失

### A. Prompt 体系差异（影响能力）

- 原型新增并使用：
  - `prompts/jd-parse-system.st`
  - `prompts/interview-question-skill-*.st`
  - `prompts/interview-question-resume-*.st`
- 当前仅有通用：
  - `prompts/interview-question-system.st`
  - `prompts/interview-question-user.st`

影响：无法按“技能模板/JD 分类”精细出题。

### B. 语音面试配置缺失

- 原型有 `voice-interview-opening.yml` 与 `app.voice-interview.*` 大量参数。
- 当前 `application.yml` 未包含语音面试完整配置域。

### C. 技能知识库资源缺失

- 原型有 `resources/skills/**`（多岗位技能模板与参考知识）。
- 当前无该目录，无法支撑 Skill 选择与自定义 JD 解析后的完整能力闭环。

### D. 其他配置差异

- 原型具备 `logback-spring.xml`、`OpenAPI` 相关配置类、部分 `Properties` 类（如 `KnowledgeBaseQueryProperties`、`ResumeAnalysisProperties`）；
- 当前对应能力未完全对齐。

---

## 4. 建议新增与修改项（落地清单）

## P0（优先级最高，建议先做）

1. 补齐 Skill 模块（后端 + 前端）
   - 后端：新增 `InterviewSkillController/Service/Properties`
   - 前端：新增 `api/skill.ts`，接入技能选择与 JD 解析入口
2. 补齐面试日程模块（后端 + 前端）
   - 后端：完整 `interviewschedule` 包（controller/service/repository/model）
   - 前端：`InterviewSchedulePage` + `api/interviewSchedule.ts` + 组件
3. 补齐语音面试模块（后端 + 前端）
   - 后端：`voiceinterview` 全模块 + WebSocket + 评估异步链路
   - 前端：语音面试页、评估页、实时字幕与音频组件

## P1（中优先级）

1. 对齐文本面试会话列表接口
   - 新增 `GET /api/interview/sessions`
2. 对齐 Prompt 体系
   - 引入 JD 解析、skill/resume 分离题目模板
3. 对齐 `resources/skills/**` 技能模板资源

## P2（增强项）

1. 对齐 `OpenAPI/Swagger`、`logback-spring.xml`、配置属性类
2. 统一前端导航与入口为“面试中心 + 统一启动弹窗”模式（按需）

---

## 5. 推荐实施顺序（两周版本）

### 第 1 阶段（3-4 天）

- 后端先补 Skill + InterviewSession list API
- 前端接入 Skill API 与面试配置流（先文字面试）

### 第 2 阶段（4-5 天）

- 后端补 interviewschedule
- 前端补 InterviewSchedulePage 与日历交互

### 第 3 阶段（4-6 天）

- 后端补 voiceinterview（含 WebSocket、评估任务）
- 前端补 VoiceInterviewPage + VoiceInterviewEvaluationPage

### 第 4 阶段（1-2 天）

- 配置、Prompt、资源对齐
- 联调与回归测试

---

## 6. 风险与注意点

1. 语音面试链路依赖实时音频与 WebSocket，部署环境（网关、超时、跨域）需提前验证。
2. Skill/JD 解析引入后，提示词与结构化输出稳定性需要专项测试。
3. 日程模块涉及时间与时区处理，前后端需统一时区和格式（ISO-8601）。
4. 你项目已有用户维度隔离，新增模块接入时需沿用现有用户上下文，避免数据串读。

