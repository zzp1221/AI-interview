# 多格式内容提取与解析

## Mermaid 图表代码
```mermaid

flowchart TD
    A[前端上传]
    B[文件处理]
    C[对象存储(RustFS)]
    D[Apache Tika]
    E[TextCleaningService<br/>(语义过滤 + 格式规范化)]
    F[数据库<br/>(结果存储)]
    G[前端轮询 获取结果]
    H[AI 分析<br/>(异步)]

    A --> B
    B --> D
    D --> E
    E --> F
    E --> H
    F --> G

    subgraph D_sub[Apache Tika Components]
        direction TB
        D_Auto[AutoDetectParser]
        D_NoOp[NoOpEmbeddedExtractor]
        D_PDF[PDFParserConfig]
        D_Body[BodyContentHandler]
    end
    
    D --- D_sub

```